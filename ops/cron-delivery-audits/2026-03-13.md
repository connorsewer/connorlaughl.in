# Cron proactive-delivery audit — 2026-03-13

Run time: 2026-03-13 09:01 America/Chicago
Scope: delivery/routing mismatches only; no job changes made.

## Top mismatches found

1. **Daily Gmail triage (jobs/shipping)** (`2278b8ae-7502-4fea-b5de-7ca45895034d`)
   - Why flagged: payload says "After run, send a brief summary with counts" and the job name reads like a user-facing daily summary.
   - Current config: `sessionTarget=isolated`, `delivery.mode=none`.
   - Risk: likely intended to proactively brief Connor, but currently cannot deliver anything externally.

2. **Delta Brief (6:00am CT daily)** (`8e8e08fc-d4f2-4b66-a00b-50473df4687d`)
   - Why flagged: name explicitly says "Delta Brief" and payload says "Send Connor the daily Delta Brief".
   - Current config: `sessionTarget=isolated`, `delivery.mode=none`.
   - Note: payload references email via `notify_owner.py`, but from a cron-delivery perspective this is still a mismatch because the cron itself is configured as internal-only despite being clearly user-facing.
   - Risk: silent failure to proactively reach Connor if the script does not independently handle delivery or if that path breaks.

3. **Swarm Summary (AM/PM) — what ran + what shipped** (`e3c257da-33f7-43cb-a7ce-766efa8ea2bd`)
   - Why flagged: payload says to compose a Telegram summary and send via the `message` tool.
   - Current config: `sessionTarget=isolated`, `delivery.mode=none`.
   - Risk: cron relies on in-task manual messaging instead of top-level delivery config; this is the exact class of misrouting that previously caused proactive updates to fail. It may work today, but the routing is brittle and inconsistent with the proactive-delivery pattern.

## Near-miss / likely intentional but worth a glance

- **INBOX GUARDIAN — real-time email monitoring** (`104f920b-9d5f-428e-b10a-d98eb18b8f8b`)
  - Payload says immediate Telegram alert only for urgent cases, otherwise `NO_REPLY`.
  - `delivery.mode=none` may be intentional if the agent uses explicit message-sending in urgent branches, but it follows the same brittle pattern.

## Not flagged as mismatches

- `30m project update notifier` and `OpenClaw proactive watchdog` are correctly configured with isolated `agentTurn` + explicit Telegram announce delivery.
- Disabled historical `sessionTarget=main` reminder (`NorthShore billing reminder`) fits the audit pattern conceptually, but it is disabled and already past-due, so not treated as an active mismatch.

## Recommendation

Convert clearly proactive jobs to the standard pattern:
- `sessionTarget=isolated`
- `payload.kind=agentTurn`
- top-level `delivery.mode=announce`
- explicit `channel=telegram` and `to=7879720980`

Avoid depending on ad-hoc in-payload "send via message tool" instructions for core proactive notifications.
