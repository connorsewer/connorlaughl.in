# Cron proactive-delivery audit — 2026-03-14

Reviewed cron jobs for delivery/routing mismatches where the job appears intended to proactively notify Connor but is configured as internal-only (`delivery.mode=none`) or otherwise lacks user-facing delivery.

## Top mismatches found

1. **Daily Gmail triage (jobs/shipping)** (`2278b8ae-7502-4fea-b5de-7ca45895034d`)
   - Why flagged: payload says "After run, send a brief summary with counts."
   - Current config: `sessionTarget=isolated`, `delivery.mode=none`
   - Likely issue: asks for a user-facing summary but no delivery route exists.

2. **LinkedIn inbox monitor (hourly)** (`0e9bb909-04a5-4bae-b102-cad01953f922`, disabled)
   - Why flagged: payload explicitly says "send Connor a Telegram DM" for new messages or errors.
   - Current config: `sessionTarget=isolated`, `delivery.mode=none`
   - Likely issue: proactive alert intent is explicit, but delivery is disabled.

3. **Delta Brief (6:00am CT daily)** (`8e8e08fc-d4f2-4b66-a00b-50473df4687d`, disabled)
   - Why flagged: name includes "Brief" and payload begins "Send Connor the daily Delta Brief".
   - Current config: `sessionTarget=isolated`, `delivery.mode=none`
   - Likely issue: clearly intended as proactive user delivery, but configured internal-only.

4. **Swarm Summary (AM/PM) — what ran + what shipped** (`e3c257da-33f7-43cb-a7ce-766efa8ea2bd`, disabled)
   - Why flagged: name includes "Summary" and payload instructs explicit Telegram sending via message tool.
   - Current config: `delivery.mode=none`
   - Likely issue: relying on in-job tool sending instead of cron delivery, which is the exact pattern this audit is meant to catch as brittle/misaligned.

5. **Ocean's 11 Batch 2 Activation** (`ab918ce0-b04c-40e8-8724-ed910d30754d`, disabled)
   - Why flagged: payload explicitly says "Send Telegram notification".
   - Current config: `delivery.mode=none`
   - Likely issue: user-facing notification requested, but cron delivery is disabled.

6. **Agent Heartbeat Monitor — 5 min** (`b3aac34e-8b15-471c-b450-f01357400da9`, disabled)
   - Why flagged: payload explicitly says "Send Telegram alert" for stale agents.
   - Current config: `delivery.mode=none`
   - Likely issue: proactive alert intent is explicit, but routing is internal-only.

## Notes

- I did **not** flag jobs whose payloads explicitly say no external messaging or whose summaries appear intended for local files only.
- I also did **not** flag `sessionTarget=main` reminders that were obviously designed as internal main-session reminders rather than off-device proactive notifications.
- Several disabled jobs still show clear proactive-delivery mismatches; they matter if re-enabled later.

## Suggested remediation pattern

For jobs meant to reach Connor while away from the MacBook:
- prefer `sessionTarget=isolated`
- use `payload.kind=agentTurn`
- set `delivery.mode=announce`
- set `delivery.channel=telegram`
- set `delivery.to=7879720980` (or the intended target)

Do not auto-fix without review, since some jobs may intentionally rely on in-run tool sends or may now be obsolete.
