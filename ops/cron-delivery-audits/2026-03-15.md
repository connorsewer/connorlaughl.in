# Cron proactive-delivery audit — 2026-03-15

Time: 2026-03-15 09:02 CT
Scope: delivery/routing mismatches only (no automatic fixes)

## Top mismatches found

1. **Daily Gmail triage (jobs/shipping)** (`2278b8ae-7502-4fea-b5de-7ca45895034d`)
   - Current config: `sessionTarget=isolated`, `delivery.mode=none`
   - Why it looks mismatched: payload explicitly says "After run, send a brief summary with counts."
   - Risk: summary is likely intended for Connor, but current config suppresses delivery.

2. **Delta Brief (6:00am CT daily)** (`8e8e08fc-d4f2-4b66-a00b-50473df4687d`)
   - Current config: `sessionTarget=isolated`, `delivery.mode=none`
   - Why it looks mismatched: name includes "Brief" and payload says "Send Connor the daily Delta Brief".
   - Risk: clearly user-facing brief is configured as internal-only/no-delivery.

3. **Session Cleanup (72h)** (`032f3bc2-c8e7-4c28-b4b4-2b9ec7fbc812`)
   - Current config: `sessionTarget=isolated`, `delivery.mode=none`
   - Why it looks mismatched: payload says "Run session cleanup script and report results."
   - Risk: "report results" suggests intended user-facing outcome, but no delivery path exists.

4. **Silent Backup (6h)** (`f4d9337a-60ec-4ba9-8c7f-2649b2e84f33`)
   - Current config: `sessionTarget=isolated`, `delivery.mode=none`
   - Why it looks mismatched: payload says "Only report if changes were pushed."
   - Risk: conditional proactive report appears intended, but no delivery path exists.

5. **DISK SPACE PREVENTIVE CLEANUP** (no id shown in listing)
   - Current config: `sessionTarget=isolated`, `delivery.mode=none`
   - Why it looks mismatched: payload says "Report disk space after."
   - Risk: report appears intended for Connor or another user-facing surface, but no delivery is configured.

## Secondary mismatches / worth reviewing

6. **LinkedIn inbox monitor (hourly)** (`0e9bb909-04a5-4bae-b102-cad01953f922`)
   - Current config: `delivery.mode=none`
   - Payload explicitly says to "send Connor a Telegram DM" on new messages or errors.
   - Strong user-facing intent, but disabled and misrouted if re-enabled without fixing delivery.

7. **Swarm Summary (AM/PM) — what ran + what shipped** (`e3c257da-33f7-43cb-a7ce-766efa8ea2bd`)
   - Current config: `delivery.mode=none`
   - Payload explicitly instructs sending via the `message` tool, then output `NO_REPLY`.
   - This may work operationally, but from a scheduler-delivery perspective it is a mismatch: user-facing delivery is embedded in the prompt instead of being represented in cron delivery config.

8. **Agent Heartbeat Monitor — 5 min** (`b3aac34e-8b15-471c-b450-f01357400da9`)
   - Current config: `delivery.mode=none`
   - Payload says to "Send Telegram alert" for stale agents.
   - Similar to above: prompt implies proactive user messaging without scheduler delivery config.

9. **Ocean's 11 Batch 2 Activation** (`ab918ce0-b04c-40e8-8724-ed910d30754d`)
   - Current config: `delivery.mode=none`
   - Payload explicitly says "Send Telegram notification".
   - Disabled/one-shot historical job, but if reused this is misconfigured for proactive delivery.

10. **Weekly skill deployment review (self-improvement)** (`853b6995-6802-44c9-b261-60a3712d91b4`)
   - Current config: `delivery.mode=announce`
   - Payload says "No external sends. Keep summary under 1200 chars."
   - This is the inverse mismatch: scheduler delivery is enabled while prompt text says no external sends.

11. **Memory Steward Sync (4hr)** (`6f371b41-49ab-41ab-b39d-94721bd47ce9`)
   - Current config: `delivery.mode=announce`
   - Payload says "No external messaging."
   - Another inverse mismatch between prompt instructions and delivery config.

12. **Facts DB: Calendar synthesis (low-noise)** (`6c56db15-deb7-4702-b0ac-ef7d3d657ba9`)
   - Current config: `delivery.mode=announce`
   - Payload says "No external messaging."
   - Likely accidental delivery config drift.

## Notes

- I focused on jobs whose names/payloads strongly suggest proactive user notification or explicit reporting to Connor.
- I did **not** change any cron jobs.
- Most active/high-confidence issues are the first five above, with the strongest two being **Daily Gmail triage** and **Delta Brief** because their payloads directly instruct sending a user-facing summary/brief while delivery is `none`.
